import * as React from 'react';
import Layout from '../components/layout';

const ResumePage = () => {
  return (
    <Layout pageTitle='Resume Page'>
      <p>This is the Resume page!</p>
    </Layout>
  );
};

export default ResumePage;
